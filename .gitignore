node_modules/
.next/
out/
build/
next-env.d.ts
*.tsbuildinfo

.env*
!.env.example

.DS_Store
Thumbs.db

.claude/

# public/images/ IS committed — roughly 6.5MB of CC0 / Creative Commons
# photography. It has to be in the repo or a build host deploys a site with no
# pictures, since nothing runs `npm run images` during a build.
